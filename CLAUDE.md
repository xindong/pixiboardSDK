# 项目协作约定

- 本仓库当前是 PixiBoardJS SDK 的架构与交付计划项目，`docs/` 是主要交付物。
- 所有架构判断应追溯到 `../pixi-board` 的现有代码或明确标记为目标设计。
- 已确认的长期决策写入 `docs/adr/`；未确认事项写入 `docs/12-risks-open-decisions.md`。
- 迁移计划必须保持现有桌面应用可逐阶段运行，避免“大爆炸式”替换。
- SDK 文档模型保持扁平；除非新增 ADR 改变决定，不引入通用场景树。
- 对外默认只有一个主包 `pixiboardjs`，内部包不等于都对外发布。

